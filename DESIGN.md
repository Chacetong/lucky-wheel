# Lucky Wheel 视觉设计规范

## 设计方向

**风格名称：IMPACT DRAW — 高冲击平面运动主义**

Lucky Wheel 不是仪表盘、音乐播放器或传统抽奖网站。它应当像一张正在运转的运动广告：直接、强烈、充满动势，把每次抽奖塑造成一次冠军揭晓。

视觉参考来自现代运动品牌广告中的尺度、留白与排版张力，但不复制任何品牌标识、字体组合或具体版式。

### 核心表达

> 黑白负责力量，绿色负责胜利，转盘负责运动。

### 品牌气质

- **动势**：画面像即将冲出边界。
- **巨构**：标题、转盘和结果采用极具冲击力的尺度。
- **果断**：使用纯色、硬边和强对比，不依赖玻璃或阴影制造质感。
- **克制**：黑白占据主要画面，绿色只用于关键状态。

### 反向约束

界面不得呈现以下感觉：

- 仪表盘、后台或控制台
- Spotify 式深色卡片布局
- 赛博朋克、霓虹灯或游戏直播间
- 赌场、老虎机或廉价抽奖网站
- 通用 SaaS、玻璃拟态或 AI 生成模板
- 编辑杂志式衬线字体与小号说明标签

## 核心视觉概念

转盘是产品功能，也是页面的主视觉素材。它不应被收纳在一张独立卡片中，而应直接参与整个版面构图，允许局部越界、被裁切或与巨型文字叠合。

页面的记忆点应当是：

> 一个巨大的彩色转盘，被黑白巨型文字与锐利版式压在画面中央；点击后，绿色瞬间接管整个界面。

## 色彩系统

采用“极端黑白 + 瞬时绿色”的配色策略。

| Token | 色值 | 用途 |
| --- | --- | --- |
| `--black` | `#050505` | 页面主背景、大面积文字块 |
| `--white` | `#F4F4EF` | 主标题、正文、主要按钮 |
| `--green` | `#39F46A` | 开始、选中、聚焦、中奖时刻 |
| `--gray` | `#A6A6A0` | 次要说明和辅助信息 |
| `--deep-gray` | `#1A1A18` | 输入区域、分隔层和弱状态 |
| `--danger` | `#FF4D57` | 删除和错误状态 |

### 使用比例

- 黑色：约 65%
- 白色：约 25%
- 绿色：不超过 10%
- 转盘色彩不计入界面基础色比例

### 绿色的语义

绿色只用于三个状态：

1. 准备：输入聚焦、模式开启
2. 触发：开始抽奖、旋转进行中
3. 胜利：中奖结果全屏揭晓

日常界面不得大面积滥用绿色。绿色每次出现都必须代表状态变化。

### 禁止项

- 渐变文字
- 彩色光晕
- 霓虹描边
- 装饰性渐变背景
- 玻璃拟态
- 低对比灰色正文

## 字体系统

### 中文字体

使用 `Noto Sans SC` / 思源黑体，加载 400、500、700、800、900 字重。

### 英文与数字字体

使用 `Barlow Condensed`，主要用于：

- 巨型英文标语
- 参与者序号
- 权重与概率
- 短标签和状态数字

### 字体角色

| 角色 | 桌面端 | 移动端 | 字重 |
| --- | --- | --- | --- |
| Hero 标题 | `clamp(4.5rem, 9vw, 7rem)` | `clamp(2.75rem, 15vw, 4rem)` | 900 |
| 中奖者名字 | `clamp(6rem, 13vw, 10rem)` | `clamp(3.5rem, 18vw, 6rem)` | 900 |
| 区域标题 | `2rem–3rem` | `1.5rem–2rem` | 800 |
| 主按钮 | `1.125rem–1.375rem` | `1.125rem` | 800 |
| 正文与输入 | `0.875rem–1rem` | `0.875rem–1rem` | 500 |
| 数字信息 | `0.875rem–1.125rem` | `0.8rem–1rem` | 700 |

### 排版规则

- Hero 标题行距控制在 `0.86–0.95`。
- 标题字距不得小于 `-0.04em`。
- 中文标题通过主动换行形成画面，而不是依赖自动换行。
- 正文不得使用全大写。
- 英文全大写只用于极短标语和状态词。
- 巨型文字可以被画面边缘裁切，但必须保持可识别。

## 构图系统

### 桌面端

使用不对称的平面广告构图：

- 左上或左侧放置巨型品牌文案。
- 转盘占据画面主要面积，位置略微偏离几何中心。
- 转盘允许越过传统内容边界。
- 名单编辑区放在右侧或底部，作为密集的信息带。
- 主要操作按钮横跨页面或形成明确的实心色块。
- 区域之间通过留白、比例和实线分隔，不使用卡片堆叠。

### 移动端

移动端必须重新编排，而不是简单压缩桌面版：

1. 巨型标题
2. 转盘
3. 全宽开始按钮
4. 名单编辑区域

首屏必须同时暴露转盘主体和抽奖入口。

### 空间规则

- 页面允许明显的疏密对比。
- Hero 区域使用大尺度留白。
- 名单区域可以更紧凑，但每个触控目标不小于 44px。
- 不使用多个同尺寸、同圆角的容器。
- 不使用嵌套卡片。

## 图形语言

允许使用：

- 粗实线
- 硬边矩形
- 单次出现的斜切构图
- 超大数字
- 被裁切的文字
- 轮廓字与实心字的有限对比
- 大面积纯色切换

禁止使用：

- 重复斜纹背景
- 装饰性网格背景
- 多层柔和阴影
- 大面积模糊
- 无意义的小标签和技术参数
- 手绘涂鸦或伪工业元素

## 转盘设计

转盘是页面中唯一允许完整多彩的对象。

### 静止状态

- 转盘尺寸接近可用画面高度。
- 使用平面纯色扇区。
- 外轮廓采用明确的黑色或白色粗线。
- 去除娱乐场所灯泡边框和持续闪烁。
- 中心指针采用绿色几何形状。
- 参与者标签更粗、更短，优先保证识别。

### 旋转状态

- 页面其他信息降低对比度。
- 转盘成为唯一持续运动的对象。
- 旋转继续使用缓出曲线，建立悬念。
- 不叠加光污染、粒子轨迹或无意义的镜头抖动。

## 名单编辑区

名单应像比赛阵容或制作名单，而不是设置面板。

推荐结构：

```text
01  PLAYER ONE       ×1.0
02  PLAYER TWO       ×1.0
03  PLAYER THREE     ×1.0
```

### 规则

- 每位参与者拥有明确序号。
- 名称输入框默认接近普通文本。
- 聚焦时才显示绿色边框或底线。
- 权重以 `×1.0` 的形式表达。
- 删除操作默认弱化，悬停或聚焦时增强。
- 添加参与者使用 `+ ADD PLAYER` 或对应中文。
- 不为每一行创建独立圆角卡片。

## 主要操作按钮

建议文案：**立即开转 →**

### 默认状态

- 白底黑字
- 硬边矩形
- 大字号、粗字重
- 占据明显的横向面积

### 悬停状态

- 绿底黑字
- 快速、直接的颜色切换
- 可以产生不超过 2px 的位移

### 按下状态

- 轻微压缩
- 不使用彩虹、星光或波纹动画

### 抽奖状态

- 文案切换为“正在决定…”
- 按钮降低交互感，但保持布局稳定

## 中奖揭晓

中奖结果使用全屏接管，不使用居中小弹窗。

推荐排版：

```text
THE
WINNER
IS

PLAYER 03
```

### 揭晓规则

- 背景从黑色切换为绿色。
- 中奖者名字占据主要画面。
- 中奖者对应的转盘颜色可以作为局部色块。
- 中奖概率放在边缘，作为辅助信息。
- “再抽一次”使用黑底白字的大按钮。
- 关闭操作清晰但不抢夺主操作。
- 彩纸数量有限，快速出现并快速结束。

## 动效系统

只保留三个主要动效时刻。

### 1. 页面进入

- 标题和转盘从不同方向快速就位。
- 动画只发生一次。
- 内容默认可见，动画不得阻塞页面阅读。

### 2. 转盘旋转

- 转盘承担主要运动。
- 其他内容降低对比度或短暂静止。
- 使用 `ease-out-quint` 或 `ease-out-expo`。

### 3. 结果揭晓

- 绿色全屏接管。
- 中奖者名字通过快速裁切或位移出现。
- 动画完成后界面立即恢复可操作状态。

### 通用规则

- 普通状态过渡控制在 150–250ms。
- 不动画布局尺寸、外边距等高开销属性。
- 不使用弹簧、弹跳或橡皮筋效果。
- 不运行永久循环的装饰动画。
- 必须支持 `prefers-reduced-motion`。

## 表面与控件

- 页面主区域不使用卡片式容器。
- 输入框圆角为 `0–4px`。
- 主按钮使用硬边矩形。
- 标准开关可以保留胶囊形。
- 阴影只用于需要脱离页面层级的临时元素，模糊半径不超过 8px。
- 分隔线使用纯白低透明度或深灰实线。

## 文案风格

文案应短、主动、有力量。

推荐：

- 立即开转 →
- 谁会赢？
- 正在决定…
- 胜者诞生
- 再来一次
- 添加选手

避免：

- 冗长功能说明
- 过多感叹号
- 赌场或博彩用语
- 伪技术参数
- 轻佻、幼稚或卡通式表达

## 可访问性

- 正文与背景对比度至少达到 4.5:1。
- 大号粗体文字至少达到 3:1。
- 所有交互元素必须具有清晰的键盘焦点状态。
- 颜色不能是传达状态的唯一方式。
- Canvas 必须提供可理解的替代文本。
- 全屏中奖界面需要焦点管理、Escape 关闭和焦点锁定。
- 移动端触控目标最小为 44×44px。

## 验收标准

完成后的界面必须满足：

1. 第一眼看到的是巨型排版和转盘，而不是容器或设置面板。
2. 页面中不存在两个并排的圆角卡片。
3. 黑白构成主体，绿色只在关键状态出现。
4. 转盘是唯一完整多彩的视觉对象。
5. 开始按钮具有广告标题般的视觉重量。
6. 中奖结果是全屏事件，而不是普通弹窗。
7. 名单编辑仍然快速、清晰且适合键盘操作。
8. 桌面与移动端都保持视觉冲击力，不发生文字溢出。
9. 不存在永久装饰动画、过度模糊或宽泛阴影。
10. 即使移除所有动画，静态画面依然成立。

## 最终判断

如果界面看起来像一个“精致的抽奖工具”，说明设计仍然不够大胆。

正确的结果应当像一场正在发生的冠军揭晓，只是它恰好也能编辑名单并完成抽奖。
